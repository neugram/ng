type t struct {
	x int64
	y float64
}

v := t{3, 1.2}

r := int64(float64(v.x) - v.y)

/*
func min(x, y num) {
	if x < y {
		return x
	}
	return y
}

func max(x, y num) {
	if x < y {
		return y
	}
	return x
}

r = max(min(r, 4), 1)
*/

if r == 2 {
	print("OK")
}
