type t struct {
	X int64
	Y float64
}

v := t{3, 1.2}

r := int64(float64(v.X) - v.Y)

/*
func min(x, y num) {
	if x < y {
		return x
	}
	return y
}

func max(x, y num) {
	if x < y {
		return y
	}
	return x
}

r = max(min(r, 4), 1)
*/

if r == 1 {
	print("OK")
}
